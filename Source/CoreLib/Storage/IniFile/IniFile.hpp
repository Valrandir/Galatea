#pragma once
#include "../TextFile.hpp"
#include "../../String/String.hpp"

namespace Core
{
	namespace Storage
	{
		class IniFile
		{
			TextFile* _file;

			//IniFile(File* file);
			//IniFile(IniFile const &);
			//IniFile& operator=(IniFile&);

			public:
		};
	}
}
